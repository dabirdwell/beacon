# Your domain (must point to this server's IP before running setup)
DOMAIN=stream.yourgroup.org

# Email for SSL certificate registration (Let's Encrypt)
EMAIL=you@example.com

# Password for the /status page (admin only — viewers don't need this)
ADMIN_PASSWORD=changeme

# YouTube stream key (optional — leave blank to use self-hosted only)
YOUTUBE_KEY=
